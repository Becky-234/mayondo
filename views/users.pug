extends layout.pug 
block styles
  link(rel="stylesheet", href="/css/user.css")  
block content 
  include sidebar.pug
  main.main-content
    //- nav.navbar.bg-body-tertiary.dashboard-header
    //-   .container-fluid.d-flex.justify-content-end.align-items-center
    //-     .d-flex.align-items-center
    //-       a.logoutBtn(href="/index") Logout
    // SUCCESS MODAL
    if success
      .modal.fade.show(tabindex='-1', style='display: block; background-color: rgba(0,0,0,0.5);')
        .modal-dialog.modal-dialog-centered
          .modal-content
            .modal-header.bg-success.text-white
              h5.modal-title
                i.fas.fa-check-circle.me-2
                | Success!
              button.btn-close(type='button', onclick="closeMessageModal()")
            .modal-body
              p #{success}
            .modal-footer
              button.btn.btn-success(type='button', onclick="closeMessageModal()") OK

    // ERROR MODAL
    if error
      .modal.fade.show(tabindex='-1', style='display: block; background-color: rgba(0,0,0,0.5);')
        .modal-dialog.modal-dialog-centered
          .modal-content
            .modal-header.bg-danger.text-white
              h5.modal-title
                i.fas.fa-exclamation-circle.me-2
                | Error!
              button.btn-close(type='button', onclick="closeMessageModal()")
            .modal-body
              p #{error}
            .modal-footer
              button.btn.btn-danger(type='button', onclick="closeMessageModal()") OK

    // DELETE CONFIRMATION MODAL
    #deleteConfirmationModal.modal.fade(tabindex='-1', aria-labelledby='deleteConfirmationModalLabel', aria-hidden='true')
      .modal-dialog.modal-dialog-centered
        .modal-content
          .modal-header.bg-warning.text-dark
            h5#deleteConfirmationModalLabel.modal-title
              i.fas.fa-exclamation-triangle.me-2
              | Confirm Deletion
            button.btn-close(type='button', data-bs-dismiss='modal', aria-label='Close')
          .modal-body
            p#deleteModalText Are you sure you want to delete this user?
            p.text-muted.small This action cannot be undone.
          .modal-footer
            button.btn.btn-secondary(type='button', data-bs-dismiss='modal') Cancel
            button#confirmDeleteBtn.btn.btn-danger(type='button') Delete

    // HIDDEN INPUT FOR USER ID
    input#deleteUserId(type='hidden')
    
    .to-do
      a#newUser.btns(href="/add") Add New User
      a#downloadExcel.btns Download Excel
      a#downloadPdf.btns Download PDF
      input#searchUser(type="search", placeholder="Search...")

    .table-container
      table#usersTable.table-content
        thead
          tr
            th Full Name
            th Email
            th Phone Number
            th NIN
            th Address
            th Username 
            th Password 
            th Date
            th Actions 
        tbody
          if users.length
            each user in users
              tr
                td #{user.name}
                td #{user.email}
                td +256#{user.tel}
                td #{user.nin}
                td #{user.address}
                td #{user.username}
                td #{user.password}
                td #{new Date(user.date).toLocaleDateString()}
                td
                  a.btn-icon(href=`/editUser/${user._id}` title='Edit')
                    i.fas.fa-edit
                  form(method='POST', action=`/deleteUser/${user._id}`, style='display:inline', onsubmit="return confirm('Are you sure you want to delete this User?')")
                    button.btn-icon(type='submit', title='Delete',  data-user-id=user._id,  data-user-name=user.name)
                      i.fas.fa-trash
          else
            tr
              td(colspan="9", style="text-align: center;") No users found
    
    h5#notFound Not Found

    footer
      p © 2025 Mayondo Wood & Furniture Ltd. All rights reserved.

  script(src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js")
  script(src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js")
  script(src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/3.5.28/jspdf.plugin.autotable.min.js")
  script(src="../javascript/user.js")